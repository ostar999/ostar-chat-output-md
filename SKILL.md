---
name: ostar-chat-output-md
description: 导出当前会话聊天记录为markdown格式。触发词：导出聊天记录为markdown,导出聊天记录.
---

# 导出原则

- 导出路径：默认导出路径为本机`~/Downloads/`目录，注意不是对话项目目录。如果用户有特别制定输出路径，以用户命令为准。

- 默认导出格式：markdown格式。除非用户有另外特别的要求，则以用户命令为准。
- 导出内容：默认导出会话完整聊天记录，不做任何总结。除非用户有另外特别的要求，则以用户命令为准。

